// Prevent us from attempting to use variables
// that are not declared
"use strict"
/* comment out
//My take
// Get the current time and define variables
const now = new Date();
const hours = now.getHours();
const minutes = now.getMinutes();
let healthPoints;

// Use the time in an if statement
if (hours >= 9 && hours < 11) {
  console.log("In the morning, health = 11");
  healthPoints = 11;
} else if (hours >= 11 && hours < 17) {
        console.log("In the afternoon, health = 4");
        healthPoints = 4;
        console.log (healthPoints);
    }
   else {
        console.log("end of day, health = 1")
        healthPoints = 1;
        }

//let healthPoints = 5;

if (healthPoints >= 10) {
    console.log("Hero is healthy.");
} else if (healthPoints < 10 && healthPoints >= 5) {
    console.log("Hero is slightly damaged.");
} else {
    console.log("Hero is close to death. Needs the arms of Morpheus.");
}
*/